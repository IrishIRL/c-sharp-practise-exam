@page
@model ExamWebApp.Pages_RecipeIngredients.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RecipeIngredient[0].Recipe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecipeIngredient[0].Ingredient)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecipeIngredient[0].Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RecipeIngredient) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Recipe!.ReсipeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ingredient!.IngredientName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Model.GetMeasurement(item.MeasurementId).Result
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
